<?xml version="1.0" encoding="UTF-8"?><!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zeroone.star.storemanagement.mapper.OtherOutListInfoMapper">
    <!-- 批量插入明细 -->
    <insert id="insertBatch">
        INSERT INTO  extry_info(
        pid, goods, attr, unit, warehouse,
        batch, mfd, price, nums, serial,
        total, data
        ) VALUES
        <foreach collection="list" item="item" separator=",">
            (
            #{item.pid}, #{item.goods}, #{item.attr}, #{item.unit}, #{item.warehouse},
            #{item.batch}, #{item.mfd}, #{item.price}, #{item.nums}, #{item.serial},
            #{item.total}, #{item.data}
            )
        </foreach>
    </insert>

    <!-- 根据主表ID查询明细 -->
    <select id="selectByPid" parameterType="java.lang.String"
            resultType="com.zeroone.star.storemanagement.entity.ExtryInfoDO">
        SELECT
            id, pid, goods, attr, unit, warehouse,
            batch, mfd, price, nums, serial, total, data
        FROM extry_info
        WHERE pid = #{pid}
        ORDER BY id ASC
    </select>
</mapper>